-- 024: витрина «кружки по странам» на странице идей (/app/inspiration).
--
-- Зачем колонка. Стиль каталожной вещи взять неоткуда: 22193 из 22418 строк
-- wardrobe_items несут литерал 'Casual' — дефолт импортёра, а не разметку
-- (замер 2026-08-13, см. комментарий в backend/app/api/cron.py:723). Поэтому
-- «страна» живёт не на вещи, а на собранном образе: витрину наполняет
-- backend/scripts/seed_vibes.py, а не разметка товаров.
--
-- Почему это вообще нужно. В outfits сейчас 16 строк, все у одного user_id —
-- это весь пул «готовых образов». Резать 16 образов на шесть кружков нельзя,
-- поэтому колонка появляется вместе со скриптом наполнения, а не до него.
--
-- Контракт владельца: у курируемых образов user_id = нули. FK на
-- outfits.user_id нет (проверено на проде), поэтому «ничейный» uuid валиден, а
-- GET /api/outfits фильтрует по user_id — значит курируемые образы не попадут
-- ни в чей раздел «мои образы». Признак курируемости — vibe IS NOT NULL.
--
-- Индекса нет намеренно: витрина — это сотни строк, на таком объёме планировщик
-- всё равно выберет seq scan. Добавить, когда outfits перевалит за десятки тысяч.

BEGIN;
ALTER TABLE outfits ADD COLUMN IF NOT EXISTS vibe text;
COMMIT;
