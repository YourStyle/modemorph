fastapi>=0.111.0
uvicorn[standard]>=0.29.0
torch>=2.0
torchvision>=0.15.0
transformers>=4.40.0
faiss-cpu>=1.8.0
scikit-learn>=1.4.0
Pillow>=10.0.0
httpx>=0.27.0
asyncpg>=0.29.0
python-multipart>=0.0.9
numpy>=1.26.0
# gdown: fetches OutfitTransformer pre-trained checkpoint from Google Drive
# on first container start. 1.1 GB one-time download, cached in a docker volume.
gdown>=5.2.0
# rembg: background removal for /clip/remove-bg. Uses ONNX runtime; default
# isnet-general-use model (~170MB) is fetched on first call and cached in
# the rembg_data volume mounted at /data/rembg (U2NET_HOME).
rembg>=2.0.50,<2.1.0
onnxruntime>=1.17.0
