map $ssl_preread_server_name $backend_addr {
    market.yandex.ru  127.0.0.1:10443;
    default           127.0.0.1:8443;
}

server {
    listen 443;
    proxy_pass $backend_addr;
    ssl_preread on;
    proxy_connect_timeout 5s;
    proxy_timeout 120s;
}
